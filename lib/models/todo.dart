
//   int get percent =>
//       ((tasks.where((task) => task.completed).length / tasks.length) * 100)
//           .round();

//   Todo(
//     this.title,
//     this.tasks,
//   );

//   Todo.copy(Todo from)
//       : this(
//           (from.title),
//           from.tasks.map((task) => Task.copy(task)).toList(),
//         );
// }
